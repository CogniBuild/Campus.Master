import { DateFormat1Directive } from './date-format1.directive';

describe('DateFormat1Directive', () => {
  it('should create an instance', () => {
    const directive = new DateFormat1Directive();
    expect(directive).toBeTruthy();
  });
});
