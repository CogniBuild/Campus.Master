import { DateFormat2Directive } from './date-format2.directive';

describe('DateFormat2Directive', () => {
  it('should create an instance', () => {
    const directive = new DateFormat2Directive();
    expect(directive).toBeTruthy();
  });
});
